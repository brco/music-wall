<% @posts.each do |post| %>
  <div class="post">
    <h3><a href="/posts/<%= post.id %>"><%= post.title %></a> <small>(by: <%= post.author %>)</small></h3>
    <p>
      <%= post.url %>
    </p>
  </div>
<% end %>

